<h1>Tasks#index</h1>
<p></p>
<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.details %></p>

  <%= link_to task.name, task %>

  <%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>


